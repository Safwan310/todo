import React from "react";

const WelcomScreen = () => {
  return (
    <div>
      <h1>Welcome to DoDo</h1>
      <button className="bg-secondary">Add a Project</button>
    </div>
  );
};

export default WelcomScreen;
